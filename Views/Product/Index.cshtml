@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@model List<Product_Management.Models.DomainModels.Product>

<partial name="_Notification" />

<div class="container-fluid px-4">
    <div class="row">
        <div class="col-6">
            <h3 class="mb-4">PRODUCT LIST</h3>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-controller="Product" asp-action="Add">
                <i class="bi bi-plus-circle"></i>
                ADD NEW PRODUCT
            </a>
        </div>
    </div>
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                @if (Model.Count == 0)
                {
                    <h1 class="text-center">NO PRODUCT ADDED AS OF NOW!</h1>
                }
                else
                {
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th scope="col">Product Name</th>
                                <th scope="col">Description</th>
                                <th scope="col">Price (in Rs.)</th>
                                <th scope="col">Category</th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var product in Model)
                            {
                                <tr>
                                    <td>@product.ProductName</td>
                                    <td>@product.ProductDesc</td>
                                    <td>@product.ProductPrice</td>
                                    <td></td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a asp-controller="Product" asp-action="Update" asp-route-id="@product.ProductId" class="btn btn-sm btn-warning mx-2">
                                                <i class="bi bi-pencil-square"></i> Update
                                            </a>
                                            <a asp-controller="Product" asp-action="Index" class="btn btn-sm btn-danger">
                                                <i class="bi bi-trash-fill"></i> Deactive
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    </div>
</div>